<view class="swiper">
    <swiper class="myswiper" 
    indicator-dots="{{ indicatorDots }}" 
    autoplay="{{ autoplay }}" 
    interval="{{ interval }}" 
    duration="{{ duration }}" 
    indicator-color="#fff" 
    indicator-active-color="#ec6c5f">

        <block wx:for="{{ swiperData }}" wx:key="index">
        <!-- 循环 -->
            <swiper-item>
                <image mode="widthFix" src="{{ item.url }}"></image>
            </swiper-item>
        </block>
    </swiper>
</view>